"""Business services coordinating local models, documents, and knowledge.

Routes delegate domain work here so HTTP concerns remain separate from local
Ollama, controlled storage, parsing, and vector-retrieval implementations.
"""
